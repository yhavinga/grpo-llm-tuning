# Google Cloud Project ID
GCP_PROJECT="your-gcp-project-id"

# Desired name for your TPU VM (must be unique within the project/zone)
TPU_NAME="my-tpu-vm"

# Zone where the TPU will be created (e.g., us-central2-b, europe-west4-a)
# Ensure the zone supports your chosen accelerator type.
ZONE="us-central2-b"

# TPU accelerator type (e.g., v4-8, v3-8, v5litepod-8)
ACCELERATOR_TYPE="v4-8"

# TPU runtime version (e.g., tpu-ubuntu2204-base, v2-alpha)
RUNTIME_VERSION="tpu-ubuntu2204-base"

# (Optional) Email address for notifications when the TPU is ready.
# Leave blank or comment out if not needed. Requires 'msmtp' or similar configured.
EMAIL="your-email@example.com"

HF_TOKEN="your token"

WANDB_TOKEN="your token"